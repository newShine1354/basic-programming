# Set is a collection of well defined objects.
s = {2, 4, 2, 6, True, "Noor"}
print(s)
# Elments can not be excessed using index number.
# We can not do slicing of set because a set is an unordered collection of elements.
# Sets are unchangeable.
Noor = {} # Type is Dictionary.
print(type(Noor))
NS = set() # NS is empty set.
print(type(NS))

for values in s:
    print(values)