# def double(x):
#     return x*2
print(lambda x: x*2)
# print(double(5))

# appl is an higher order function as it take a function as a argument.
# def appl(fx, value):
#     return 6 + fx(value)
# print(appl(double, 2)) # passing double function as a function argument.
# print(appl(lambda x: x*x*x*x, 2)) # we can pass a lembda function(without name) as a function argument
# cube = lambda x: x*x*x
# print(cube(5))

# average = lambda x, y: (x + y)/2
# print(average(3, 5))
